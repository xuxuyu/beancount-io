COOKIE_KEYS=["TODO_generate_with_openssl_rand_base64_32"]
AUTH_SECRET=TODO
# Login/register JWT lifetime in minutes. Default 525600 (365 days).
# Override locally to test expiry, e.g. AUTH_JWT_EXP_MINUTES=5
AUTH_JWT_EXP_MINUTES=525600
FAVA_ADMIN_API_KEY=TODO
FAVA_API_KEY=TODO
SEND_GRID_API_KEY=TODO
MAILGUN_API_KEY=TODO
POSTGRES_BACKEND_URI=TODO
REDIS_URI=TODO

FAVA_API_URL=TODO
FAVA_API_ADMIN_USER=TODO
FAVA_API_ADMIN_PASSWORD=TODO

# Public base URL of this API server (used to construct archive download URLs)
SERVER_URL=https://api.v3.beancount.io

# BlockEden Configuration
BLOCKEDEN_ACCESS_KEY=TODO

# Optional direct OpenAI/OpenAI-compatible provider. Include the API version
# path (normally /v1). Use responses for PDF URL inputs.
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-4o
OPENAI_API_MODE=responses

# Admin API authentication token (also used for Claude Code Sandbox)
# Generate with: openssl rand -base64 32
ADMIN_TOKEN=TODO

# Claude Code Sandbox Configuration
# The sandbox exposes /beancount-stream endpoint for SSE streaming
# Set to your sandbox deployment URL, e.g. https://sandbox.beancount.io
CLAUDE_CODE_SANDBOX_URL=

# Stripe Configuration (Production)
STRIPE_PUBLIC_KEY=pk_live_TODO
STRIPE_PRIVATE_KEY=sk_live_TODO
STRIPE_WEBHOOK_SECRET=whsec_TODO

# Stripe Configuration (Development/Test)
STRIPE_DEV_PUBLIC_KEY=pk_test_TODO
STRIPE_DEV_PRIVATE_KEY=sk_test_TODO
STRIPE_DEV_WEBHOOK_SECRET=whsec_TODO

# Discourse SSO Configuration
DISCOURSE_SSO_SECRET=TODO
DISCOURSE_URL=https://your-discourse-instance.com

# Identity OAuth 2.1 provider — static "discourse" client for the forum's
# "Log in with Beancount" button (features/oauth/api/oidc-route.ts).
# client_id/redirect_uri/issuer are hardcoded in src/config/config.ts (not
# sensitive, and hardcoding removes deploy-config error — see that file's
# comments for why OAUTH_ISSUER specifically must never come from an env var
# again). This is the ONLY var this feature reads. A real secret is required
# (not public/PKCE-only) — discourse-openid-connect always sends client
# authentication, and oidc-provider rejects an empty secret rather than
# treating it as "none". Leave unset to keep the client disabled — the server
# boots normally either way (buildStaticClients() just registers no client).
# Unrelated to the DISCOURSE_SSO_* vars above (DiscourseConnect, not OAuth2).
# Generate with: openssl rand -base64 32
OAUTH_DISCOURSE_CLIENT_SECRET=TODO

# S3 Configuration (Temporary Asset Uploads - Auto-deleted after 3 days)
TEMP_ASSETS_AWS_S3_REGION=us-east-1
TEMP_ASSETS_AWS_S3_BUCKET=beancount-io-uploads-dev
TEMP_ASSETS_AWS_S3_ACCESS_KEY_ID=TODO
TEMP_ASSETS_AWS_S3_SECRET_ACCESS_KEY=TODO
# Optional: Custom S3 endpoint (leave empty for AWS S3)
TEMP_ASSETS_AWS_S3_ENDPOINT=

# S3 Configuration (Permanent Asset Storage)
PERM_ASSETS_AWS_S3_REGION=us-east-1
PERM_ASSETS_AWS_S3_BUCKET=beancount-io-assets-dev
PERM_ASSETS_AWS_S3_ACCESS_KEY_ID=TODO
PERM_ASSETS_AWS_S3_SECRET_ACCESS_KEY=TODO
# Optional: Custom S3 endpoint (leave empty for AWS S3)
PERM_ASSETS_AWS_S3_ENDPOINT=

# Loki Logging Configuration (Production)
# Loki server URL - if set, logs will be sent to Grafana Loki
# Example: http://loki:3100 or https://logs.example.com
LOKI_HOST=

# Plaid Configuration
# Get credentials from: https://dashboard.plaid.com/developers/keys
PLAID_CLIENT_ID=TODO
PLAID_SECRET=TODO
PLAID_ENVIRONMENT=sandbox
PLAID_WEBHOOK_URL=https://api.v3.beancount.io/api/plaid/webhook


# --- Git over SSH proxy (ADR 0004) -----------------------------------------
# When enabled, backend-v2 terminates git over SSH itself instead of Gitea, so
# the main-only rule and every application-layer check apply to SSH as well as
# HTTP. Requires Gitea's SSH to be reachable in-network but not published.
#
# Off unless BOTH are set. Taking over the port with a host key nobody has seen
# gives every existing client the same warning a man-in-the-middle produces, so
# set SSH_PROXY_HOST_KEY to Gitea's existing host key — retrieve it with
# _infra/print-ssh-host-key.sh. It is a private key: never commit it.
SSH_PROXY_ENABLED=false
SSH_PROXY_PORT=2222
SSH_PROXY_HOST_KEY=
